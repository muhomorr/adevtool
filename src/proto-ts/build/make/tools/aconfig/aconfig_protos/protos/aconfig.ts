// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.2
//   protoc               v6.33.1
// source: build/make/tools/aconfig/aconfig_protos/protos/aconfig.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "android.aconfig";

export enum flagState {
  ENABLED = 1,
  DISABLED = 2,
  UNRECOGNIZED = -1,
}

export function flagStateFromJSON(object: any): flagState {
  switch (object) {
    case 1:
    case "ENABLED":
      return flagState.ENABLED;
    case 2:
    case "DISABLED":
      return flagState.DISABLED;
    case -1:
    case "UNRECOGNIZED":
    default:
      return flagState.UNRECOGNIZED;
  }
}

export function flagStateToJSON(object: flagState): string {
  switch (object) {
    case flagState.ENABLED:
      return "ENABLED";
    case flagState.DISABLED:
      return "DISABLED";
    case flagState.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export enum flagPermission {
  READ_ONLY = 1,
  READ_WRITE = 2,
  UNRECOGNIZED = -1,
}

export function flagPermissionFromJSON(object: any): flagPermission {
  switch (object) {
    case 1:
    case "READ_ONLY":
      return flagPermission.READ_ONLY;
    case 2:
    case "READ_WRITE":
      return flagPermission.READ_WRITE;
    case -1:
    case "UNRECOGNIZED":
    default:
      return flagPermission.UNRECOGNIZED;
  }
}

export function flagPermissionToJSON(object: flagPermission): string {
  switch (object) {
    case flagPermission.READ_ONLY:
      return "READ_ONLY";
    case flagPermission.READ_WRITE:
      return "READ_WRITE";
    case flagPermission.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export interface flagDeclaration {
  /**
   * Name of the flag (required)
   * See # name for format detail
   */
  name?:
    | string
    | undefined;
  /**
   * Namespace the flag belongs to (required)
   * See # namespace for format detail
   */
  namespace?:
    | string
    | undefined;
  /** Textual description of the flag's purpose (required) */
  description?:
    | string
    | undefined;
  /** Single bug id related to the flag (required) */
  bug: string[];
  /**
   * Indicates if the flag is permanently read-only and cannot be changed
   * via release configs (optional)
   * Default value false
   */
  isFixedReadOnly?:
    | boolean
    | undefined;
  /**
   * Indicates if the flag is exported and accessible beyond its originating container (optional)
   * Default value false
   */
  isExported?:
    | boolean
    | undefined;
  /** Additional information about the flag, including its purpose and form factors (optional) */
  metadata?: flagMetadata | undefined;
}

/**
 * Optional metadata about the flag, such as its purpose and its intended form factors.
 * Can influence the applied policies and testing strategy.
 */
export interface flagMetadata {
  purpose?: flagMetadata_flagPurpose | undefined;
  storage?: flagMetadata_flagStorageBackend | undefined;
}

export enum flagMetadata_flagPurpose {
  PURPOSE_UNSPECIFIED = 0,
  PURPOSE_FEATURE = 1,
  PURPOSE_BUGFIX = 2,
  UNRECOGNIZED = -1,
}

export function flagMetadata_flagPurposeFromJSON(object: any): flagMetadata_flagPurpose {
  switch (object) {
    case 0:
    case "PURPOSE_UNSPECIFIED":
      return flagMetadata_flagPurpose.PURPOSE_UNSPECIFIED;
    case 1:
    case "PURPOSE_FEATURE":
      return flagMetadata_flagPurpose.PURPOSE_FEATURE;
    case 2:
    case "PURPOSE_BUGFIX":
      return flagMetadata_flagPurpose.PURPOSE_BUGFIX;
    case -1:
    case "UNRECOGNIZED":
    default:
      return flagMetadata_flagPurpose.UNRECOGNIZED;
  }
}

export function flagMetadata_flagPurposeToJSON(object: flagMetadata_flagPurpose): string {
  switch (object) {
    case flagMetadata_flagPurpose.PURPOSE_UNSPECIFIED:
      return "PURPOSE_UNSPECIFIED";
    case flagMetadata_flagPurpose.PURPOSE_FEATURE:
      return "PURPOSE_FEATURE";
    case flagMetadata_flagPurpose.PURPOSE_BUGFIX:
      return "PURPOSE_BUGFIX";
    case flagMetadata_flagPurpose.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

/** storage backend for flag */
export enum flagMetadata_flagStorageBackend {
  /** UNSPECIFIED - unspecified, let aconfig choose one */
  UNSPECIFIED = 0,
  /** ACONFIGD - aconfigd based new storage */
  ACONFIGD = 1,
  /** DEVICE_CONFIG - device config based legacy storage */
  DEVICE_CONFIG = 2,
  /** NONE - no need for flag storage */
  NONE = 3,
  UNRECOGNIZED = -1,
}

export function flagMetadata_flagStorageBackendFromJSON(object: any): flagMetadata_flagStorageBackend {
  switch (object) {
    case 0:
    case "UNSPECIFIED":
      return flagMetadata_flagStorageBackend.UNSPECIFIED;
    case 1:
    case "ACONFIGD":
      return flagMetadata_flagStorageBackend.ACONFIGD;
    case 2:
    case "DEVICE_CONFIG":
      return flagMetadata_flagStorageBackend.DEVICE_CONFIG;
    case 3:
    case "NONE":
      return flagMetadata_flagStorageBackend.NONE;
    case -1:
    case "UNRECOGNIZED":
    default:
      return flagMetadata_flagStorageBackend.UNRECOGNIZED;
  }
}

export function flagMetadata_flagStorageBackendToJSON(object: flagMetadata_flagStorageBackend): string {
  switch (object) {
    case flagMetadata_flagStorageBackend.UNSPECIFIED:
      return "UNSPECIFIED";
    case flagMetadata_flagStorageBackend.ACONFIGD:
      return "ACONFIGD";
    case flagMetadata_flagStorageBackend.DEVICE_CONFIG:
      return "DEVICE_CONFIG";
    case flagMetadata_flagStorageBackend.NONE:
      return "NONE";
    case flagMetadata_flagStorageBackend.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export interface flagDeclarations {
  /**
   * Package to which the flag belongs (required)
   * See # package for format detail
   */
  package?:
    | string
    | undefined;
  /** List of flag_declaration objects (required) */
  flag: flagDeclaration[];
  /**
   * Container the flag belongs to (optional)
   * See # container for format detail
   */
  container?: string | undefined;
}

export interface flagValue {
  /**
   * Package to which the flag belongs (required)
   * See # package for format detail
   */
  package?:
    | string
    | undefined;
  /**
   * Name of the flag (required)
   * See # name for format detail
   */
  name?: string | undefined;
  state?: flagState | undefined;
  permission?: flagPermission | undefined;
}

export interface flagValues {
  flagValue: flagValue[];
}

export interface tracepoint {
  /** path to declaration or value file relative to $TOP */
  source?: string | undefined;
  state?: flagState | undefined;
  permission?: flagPermission | undefined;
}

export interface parsedFlag {
  /**
   * Package to which the flag belongs (required)
   * See # package for format detail
   */
  package?:
    | string
    | undefined;
  /**
   * Name of the flag (required)
   * See # name for format detail
   */
  name?:
    | string
    | undefined;
  /**
   * Namespace the flag belongs to (required)
   * See # namespace for format detail
   */
  namespace?:
    | string
    | undefined;
  /** Textual description of the flag's purpose (required) */
  description?:
    | string
    | undefined;
  /** Single bug id related to the flag (required) */
  bug: string[];
  state?: flagState | undefined;
  permission?: flagPermission | undefined;
  trace: tracepoint[];
  /**
   * Indicates if the flag is permanently read-only and cannot be changed
   * via release configs (optional)
   * Default value false
   */
  isFixedReadOnly?:
    | boolean
    | undefined;
  /**
   * Indicates if the flag is exported and accessible beyond its originating container (optional)
   * Default value false
   */
  isExported?:
    | boolean
    | undefined;
  /**
   * Container the flag belongs to (optional)
   * See # container for format detail
   */
  container?:
    | string
    | undefined;
  /** Additional information about the flag, including its purpose and form factors (optional) */
  metadata?: flagMetadata | undefined;
}

export interface parsedFlags {
  parsedFlag: parsedFlag[];
}

function createBaseflagDeclaration(): flagDeclaration {
  return {
    name: "",
    namespace: "",
    description: "",
    bug: [],
    isFixedReadOnly: false,
    isExported: false,
    metadata: undefined,
  };
}

export const flagDeclaration: MessageFns<flagDeclaration> = {
  encode(message: flagDeclaration, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.name !== undefined && message.name !== "") {
      writer.uint32(10).string(message.name);
    }
    if (message.namespace !== undefined && message.namespace !== "") {
      writer.uint32(18).string(message.namespace);
    }
    if (message.description !== undefined && message.description !== "") {
      writer.uint32(26).string(message.description);
    }
    for (const v of message.bug) {
      writer.uint32(34).string(v!);
    }
    if (message.isFixedReadOnly !== undefined && message.isFixedReadOnly !== false) {
      writer.uint32(40).bool(message.isFixedReadOnly);
    }
    if (message.isExported !== undefined && message.isExported !== false) {
      writer.uint32(48).bool(message.isExported);
    }
    if (message.metadata !== undefined) {
      flagMetadata.encode(message.metadata, writer.uint32(58).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): flagDeclaration {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseflagDeclaration();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.namespace = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.description = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.bug.push(reader.string());
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.isFixedReadOnly = reader.bool();
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.isExported = reader.bool();
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.metadata = flagMetadata.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): flagDeclaration {
    return {
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      namespace: isSet(object.namespace) ? globalThis.String(object.namespace) : "",
      description: isSet(object.description) ? globalThis.String(object.description) : "",
      bug: globalThis.Array.isArray(object?.bug) ? object.bug.map((e: any) => globalThis.String(e)) : [],
      isFixedReadOnly: isSet(object.isFixedReadOnly)
        ? globalThis.Boolean(object.isFixedReadOnly)
        : isSet(object.is_fixed_read_only)
        ? globalThis.Boolean(object.is_fixed_read_only)
        : false,
      isExported: isSet(object.isExported)
        ? globalThis.Boolean(object.isExported)
        : isSet(object.is_exported)
        ? globalThis.Boolean(object.is_exported)
        : false,
      metadata: isSet(object.metadata) ? flagMetadata.fromJSON(object.metadata) : undefined,
    };
  },

  toJSON(message: flagDeclaration): unknown {
    const obj: any = {};
    if (message.name !== undefined && message.name !== "") {
      obj.name = message.name;
    }
    if (message.namespace !== undefined && message.namespace !== "") {
      obj.namespace = message.namespace;
    }
    if (message.description !== undefined && message.description !== "") {
      obj.description = message.description;
    }
    if (message.bug?.length) {
      obj.bug = message.bug;
    }
    if (message.isFixedReadOnly !== undefined && message.isFixedReadOnly !== false) {
      obj.isFixedReadOnly = message.isFixedReadOnly;
    }
    if (message.isExported !== undefined && message.isExported !== false) {
      obj.isExported = message.isExported;
    }
    if (message.metadata !== undefined) {
      obj.metadata = flagMetadata.toJSON(message.metadata);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<flagDeclaration>, I>>(base?: I): flagDeclaration {
    return flagDeclaration.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<flagDeclaration>, I>>(object: I): flagDeclaration {
    const message = createBaseflagDeclaration();
    message.name = object.name ?? "";
    message.namespace = object.namespace ?? "";
    message.description = object.description ?? "";
    message.bug = object.bug?.map((e) => e) || [];
    message.isFixedReadOnly = object.isFixedReadOnly ?? false;
    message.isExported = object.isExported ?? false;
    message.metadata = (object.metadata !== undefined && object.metadata !== null)
      ? flagMetadata.fromPartial(object.metadata)
      : undefined;
    return message;
  },
};

function createBaseflagMetadata(): flagMetadata {
  return { purpose: 0, storage: 0 };
}

export const flagMetadata: MessageFns<flagMetadata> = {
  encode(message: flagMetadata, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.purpose !== undefined && message.purpose !== 0) {
      writer.uint32(8).int32(message.purpose);
    }
    if (message.storage !== undefined && message.storage !== 0) {
      writer.uint32(16).int32(message.storage);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): flagMetadata {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseflagMetadata();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.purpose = reader.int32() as any;
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.storage = reader.int32() as any;
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): flagMetadata {
    return {
      purpose: isSet(object.purpose) ? flagMetadata_flagPurposeFromJSON(object.purpose) : 0,
      storage: isSet(object.storage) ? flagMetadata_flagStorageBackendFromJSON(object.storage) : 0,
    };
  },

  toJSON(message: flagMetadata): unknown {
    const obj: any = {};
    if (message.purpose !== undefined && message.purpose !== 0) {
      obj.purpose = flagMetadata_flagPurposeToJSON(message.purpose);
    }
    if (message.storage !== undefined && message.storage !== 0) {
      obj.storage = flagMetadata_flagStorageBackendToJSON(message.storage);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<flagMetadata>, I>>(base?: I): flagMetadata {
    return flagMetadata.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<flagMetadata>, I>>(object: I): flagMetadata {
    const message = createBaseflagMetadata();
    message.purpose = object.purpose ?? 0;
    message.storage = object.storage ?? 0;
    return message;
  },
};

function createBaseflagDeclarations(): flagDeclarations {
  return { package: "", flag: [], container: "" };
}

export const flagDeclarations: MessageFns<flagDeclarations> = {
  encode(message: flagDeclarations, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.package !== undefined && message.package !== "") {
      writer.uint32(10).string(message.package);
    }
    for (const v of message.flag) {
      flagDeclaration.encode(v!, writer.uint32(18).fork()).join();
    }
    if (message.container !== undefined && message.container !== "") {
      writer.uint32(26).string(message.container);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): flagDeclarations {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseflagDeclarations();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.package = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.flag.push(flagDeclaration.decode(reader, reader.uint32()));
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.container = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): flagDeclarations {
    return {
      package: isSet(object.package) ? globalThis.String(object.package) : "",
      flag: globalThis.Array.isArray(object?.flag) ? object.flag.map((e: any) => flagDeclaration.fromJSON(e)) : [],
      container: isSet(object.container) ? globalThis.String(object.container) : "",
    };
  },

  toJSON(message: flagDeclarations): unknown {
    const obj: any = {};
    if (message.package !== undefined && message.package !== "") {
      obj.package = message.package;
    }
    if (message.flag?.length) {
      obj.flag = message.flag.map((e) => flagDeclaration.toJSON(e));
    }
    if (message.container !== undefined && message.container !== "") {
      obj.container = message.container;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<flagDeclarations>, I>>(base?: I): flagDeclarations {
    return flagDeclarations.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<flagDeclarations>, I>>(object: I): flagDeclarations {
    const message = createBaseflagDeclarations();
    message.package = object.package ?? "";
    message.flag = object.flag?.map((e) => flagDeclaration.fromPartial(e)) || [];
    message.container = object.container ?? "";
    return message;
  },
};

function createBaseflagValue(): flagValue {
  return { package: "", name: "", state: 1, permission: 1 };
}

export const flagValue: MessageFns<flagValue> = {
  encode(message: flagValue, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.package !== undefined && message.package !== "") {
      writer.uint32(10).string(message.package);
    }
    if (message.name !== undefined && message.name !== "") {
      writer.uint32(18).string(message.name);
    }
    if (message.state !== undefined && message.state !== 1) {
      writer.uint32(24).int32(message.state);
    }
    if (message.permission !== undefined && message.permission !== 1) {
      writer.uint32(32).int32(message.permission);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): flagValue {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseflagValue();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.package = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.state = reader.int32() as any;
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.permission = reader.int32() as any;
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): flagValue {
    return {
      package: isSet(object.package) ? globalThis.String(object.package) : "",
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      state: isSet(object.state) ? flagStateFromJSON(object.state) : 1,
      permission: isSet(object.permission) ? flagPermissionFromJSON(object.permission) : 1,
    };
  },

  toJSON(message: flagValue): unknown {
    const obj: any = {};
    if (message.package !== undefined && message.package !== "") {
      obj.package = message.package;
    }
    if (message.name !== undefined && message.name !== "") {
      obj.name = message.name;
    }
    if (message.state !== undefined && message.state !== 1) {
      obj.state = flagStateToJSON(message.state);
    }
    if (message.permission !== undefined && message.permission !== 1) {
      obj.permission = flagPermissionToJSON(message.permission);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<flagValue>, I>>(base?: I): flagValue {
    return flagValue.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<flagValue>, I>>(object: I): flagValue {
    const message = createBaseflagValue();
    message.package = object.package ?? "";
    message.name = object.name ?? "";
    message.state = object.state ?? 1;
    message.permission = object.permission ?? 1;
    return message;
  },
};

function createBaseflagValues(): flagValues {
  return { flagValue: [] };
}

export const flagValues: MessageFns<flagValues> = {
  encode(message: flagValues, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.flagValue) {
      flagValue.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): flagValues {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseflagValues();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.flagValue.push(flagValue.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): flagValues {
    return {
      flagValue: globalThis.Array.isArray(object?.flagValue)
        ? object.flagValue.map((e: any) => flagValue.fromJSON(e))
        : globalThis.Array.isArray(object?.flag_value)
        ? object.flag_value.map((e: any) => flagValue.fromJSON(e))
        : [],
    };
  },

  toJSON(message: flagValues): unknown {
    const obj: any = {};
    if (message.flagValue?.length) {
      obj.flagValue = message.flagValue.map((e) => flagValue.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<flagValues>, I>>(base?: I): flagValues {
    return flagValues.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<flagValues>, I>>(object: I): flagValues {
    const message = createBaseflagValues();
    message.flagValue = object.flagValue?.map((e) => flagValue.fromPartial(e)) || [];
    return message;
  },
};

function createBasetracepoint(): tracepoint {
  return { source: "", state: 1, permission: 1 };
}

export const tracepoint: MessageFns<tracepoint> = {
  encode(message: tracepoint, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.source !== undefined && message.source !== "") {
      writer.uint32(10).string(message.source);
    }
    if (message.state !== undefined && message.state !== 1) {
      writer.uint32(16).int32(message.state);
    }
    if (message.permission !== undefined && message.permission !== 1) {
      writer.uint32(24).int32(message.permission);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): tracepoint {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasetracepoint();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.source = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.state = reader.int32() as any;
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.permission = reader.int32() as any;
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): tracepoint {
    return {
      source: isSet(object.source) ? globalThis.String(object.source) : "",
      state: isSet(object.state) ? flagStateFromJSON(object.state) : 1,
      permission: isSet(object.permission) ? flagPermissionFromJSON(object.permission) : 1,
    };
  },

  toJSON(message: tracepoint): unknown {
    const obj: any = {};
    if (message.source !== undefined && message.source !== "") {
      obj.source = message.source;
    }
    if (message.state !== undefined && message.state !== 1) {
      obj.state = flagStateToJSON(message.state);
    }
    if (message.permission !== undefined && message.permission !== 1) {
      obj.permission = flagPermissionToJSON(message.permission);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<tracepoint>, I>>(base?: I): tracepoint {
    return tracepoint.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<tracepoint>, I>>(object: I): tracepoint {
    const message = createBasetracepoint();
    message.source = object.source ?? "";
    message.state = object.state ?? 1;
    message.permission = object.permission ?? 1;
    return message;
  },
};

function createBaseparsedFlag(): parsedFlag {
  return {
    package: "",
    name: "",
    namespace: "",
    description: "",
    bug: [],
    state: 1,
    permission: 1,
    trace: [],
    isFixedReadOnly: false,
    isExported: false,
    container: "",
    metadata: undefined,
  };
}

export const parsedFlag: MessageFns<parsedFlag> = {
  encode(message: parsedFlag, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.package !== undefined && message.package !== "") {
      writer.uint32(10).string(message.package);
    }
    if (message.name !== undefined && message.name !== "") {
      writer.uint32(18).string(message.name);
    }
    if (message.namespace !== undefined && message.namespace !== "") {
      writer.uint32(26).string(message.namespace);
    }
    if (message.description !== undefined && message.description !== "") {
      writer.uint32(34).string(message.description);
    }
    for (const v of message.bug) {
      writer.uint32(42).string(v!);
    }
    if (message.state !== undefined && message.state !== 1) {
      writer.uint32(48).int32(message.state);
    }
    if (message.permission !== undefined && message.permission !== 1) {
      writer.uint32(56).int32(message.permission);
    }
    for (const v of message.trace) {
      tracepoint.encode(v!, writer.uint32(66).fork()).join();
    }
    if (message.isFixedReadOnly !== undefined && message.isFixedReadOnly !== false) {
      writer.uint32(72).bool(message.isFixedReadOnly);
    }
    if (message.isExported !== undefined && message.isExported !== false) {
      writer.uint32(80).bool(message.isExported);
    }
    if (message.container !== undefined && message.container !== "") {
      writer.uint32(90).string(message.container);
    }
    if (message.metadata !== undefined) {
      flagMetadata.encode(message.metadata, writer.uint32(98).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): parsedFlag {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseparsedFlag();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.package = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.namespace = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.description = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.bug.push(reader.string());
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.state = reader.int32() as any;
          continue;
        }
        case 7: {
          if (tag !== 56) {
            break;
          }

          message.permission = reader.int32() as any;
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          message.trace.push(tracepoint.decode(reader, reader.uint32()));
          continue;
        }
        case 9: {
          if (tag !== 72) {
            break;
          }

          message.isFixedReadOnly = reader.bool();
          continue;
        }
        case 10: {
          if (tag !== 80) {
            break;
          }

          message.isExported = reader.bool();
          continue;
        }
        case 11: {
          if (tag !== 90) {
            break;
          }

          message.container = reader.string();
          continue;
        }
        case 12: {
          if (tag !== 98) {
            break;
          }

          message.metadata = flagMetadata.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): parsedFlag {
    return {
      package: isSet(object.package) ? globalThis.String(object.package) : "",
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      namespace: isSet(object.namespace) ? globalThis.String(object.namespace) : "",
      description: isSet(object.description) ? globalThis.String(object.description) : "",
      bug: globalThis.Array.isArray(object?.bug) ? object.bug.map((e: any) => globalThis.String(e)) : [],
      state: isSet(object.state) ? flagStateFromJSON(object.state) : 1,
      permission: isSet(object.permission) ? flagPermissionFromJSON(object.permission) : 1,
      trace: globalThis.Array.isArray(object?.trace) ? object.trace.map((e: any) => tracepoint.fromJSON(e)) : [],
      isFixedReadOnly: isSet(object.isFixedReadOnly)
        ? globalThis.Boolean(object.isFixedReadOnly)
        : isSet(object.is_fixed_read_only)
        ? globalThis.Boolean(object.is_fixed_read_only)
        : false,
      isExported: isSet(object.isExported)
        ? globalThis.Boolean(object.isExported)
        : isSet(object.is_exported)
        ? globalThis.Boolean(object.is_exported)
        : false,
      container: isSet(object.container) ? globalThis.String(object.container) : "",
      metadata: isSet(object.metadata) ? flagMetadata.fromJSON(object.metadata) : undefined,
    };
  },

  toJSON(message: parsedFlag): unknown {
    const obj: any = {};
    if (message.package !== undefined && message.package !== "") {
      obj.package = message.package;
    }
    if (message.name !== undefined && message.name !== "") {
      obj.name = message.name;
    }
    if (message.namespace !== undefined && message.namespace !== "") {
      obj.namespace = message.namespace;
    }
    if (message.description !== undefined && message.description !== "") {
      obj.description = message.description;
    }
    if (message.bug?.length) {
      obj.bug = message.bug;
    }
    if (message.state !== undefined && message.state !== 1) {
      obj.state = flagStateToJSON(message.state);
    }
    if (message.permission !== undefined && message.permission !== 1) {
      obj.permission = flagPermissionToJSON(message.permission);
    }
    if (message.trace?.length) {
      obj.trace = message.trace.map((e) => tracepoint.toJSON(e));
    }
    if (message.isFixedReadOnly !== undefined && message.isFixedReadOnly !== false) {
      obj.isFixedReadOnly = message.isFixedReadOnly;
    }
    if (message.isExported !== undefined && message.isExported !== false) {
      obj.isExported = message.isExported;
    }
    if (message.container !== undefined && message.container !== "") {
      obj.container = message.container;
    }
    if (message.metadata !== undefined) {
      obj.metadata = flagMetadata.toJSON(message.metadata);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<parsedFlag>, I>>(base?: I): parsedFlag {
    return parsedFlag.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<parsedFlag>, I>>(object: I): parsedFlag {
    const message = createBaseparsedFlag();
    message.package = object.package ?? "";
    message.name = object.name ?? "";
    message.namespace = object.namespace ?? "";
    message.description = object.description ?? "";
    message.bug = object.bug?.map((e) => e) || [];
    message.state = object.state ?? 1;
    message.permission = object.permission ?? 1;
    message.trace = object.trace?.map((e) => tracepoint.fromPartial(e)) || [];
    message.isFixedReadOnly = object.isFixedReadOnly ?? false;
    message.isExported = object.isExported ?? false;
    message.container = object.container ?? "";
    message.metadata = (object.metadata !== undefined && object.metadata !== null)
      ? flagMetadata.fromPartial(object.metadata)
      : undefined;
    return message;
  },
};

function createBaseparsedFlags(): parsedFlags {
  return { parsedFlag: [] };
}

export const parsedFlags: MessageFns<parsedFlags> = {
  encode(message: parsedFlags, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.parsedFlag) {
      parsedFlag.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): parsedFlags {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseparsedFlags();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.parsedFlag.push(parsedFlag.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): parsedFlags {
    return {
      parsedFlag: globalThis.Array.isArray(object?.parsedFlag)
        ? object.parsedFlag.map((e: any) => parsedFlag.fromJSON(e))
        : globalThis.Array.isArray(object?.parsed_flag)
        ? object.parsed_flag.map((e: any) => parsedFlag.fromJSON(e))
        : [],
    };
  },

  toJSON(message: parsedFlags): unknown {
    const obj: any = {};
    if (message.parsedFlag?.length) {
      obj.parsedFlag = message.parsedFlag.map((e) => parsedFlag.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<parsedFlags>, I>>(base?: I): parsedFlags {
    return parsedFlags.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<parsedFlags>, I>>(object: I): parsedFlags {
    const message = createBaseparsedFlags();
    message.parsedFlag = object.parsedFlag?.map((e) => parsedFlag.fromPartial(e)) || [];
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
